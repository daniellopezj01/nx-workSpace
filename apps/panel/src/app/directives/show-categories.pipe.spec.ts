import { ShowCategoriesPipe } from './show-categories.pipe';

describe('ShowCategoriesPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowCategoriesPipe();
    expect(pipe).toBeTruthy();
  });
});
