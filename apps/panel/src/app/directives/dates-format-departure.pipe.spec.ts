import { DatesFormatDeparturePipe } from './dates-format-departure.pipe';

describe('DatesFormatDeparturePipe', () => {
  it('create an instance', () => {
    const pipe = new DatesFormatDeparturePipe();
    expect(pipe).toBeTruthy();
  });
});
