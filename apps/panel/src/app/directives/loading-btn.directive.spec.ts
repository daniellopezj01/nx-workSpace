import { LoadingBtnDirective } from './loading-btn.directive';

describe('LoadingBtnDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new LoadingBtnDirective();
    expect(directive).toBeTruthy();
  });
});
