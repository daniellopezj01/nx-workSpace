import { LoadingbuttonDirective } from './loadingbutton.directive';

describe('LoadingbuttonDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingbuttonDirective();
    expect(directive).toBeTruthy();
  });
});
