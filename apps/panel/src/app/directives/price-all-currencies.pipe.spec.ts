import { PriceAllCurrenciesPipe } from './price-all-currencies.pipe';

describe('PriceAllCurrenciesPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceAllCurrenciesPipe();
    expect(pipe).toBeTruthy();
  });
});
