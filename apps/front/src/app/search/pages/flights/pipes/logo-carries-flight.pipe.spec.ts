import { LogoCarriesFlightPipe } from './logo-carries-flight.pipe';

describe('LogoCarriesFlightPipe', () => {
  it('create an instance', () => {
    const pipe = new LogoCarriesFlightPipe();
    expect(pipe).toBeTruthy();
  });
});
