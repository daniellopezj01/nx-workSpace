import { EyePasswordDirective } from './eye-password.directive';

describe('EyePasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new EyePasswordDirective();
    expect(directive).toBeTruthy();
  });
});
