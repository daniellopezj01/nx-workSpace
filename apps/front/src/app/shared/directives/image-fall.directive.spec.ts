import { ImageFallDirective } from './image-fall.directive';

describe('ImageFallDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageFallDirective();
    expect(directive).toBeTruthy();
  });
});
