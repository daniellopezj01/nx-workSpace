import { OnlyBrowserDirective } from './only-browser.directive';

describe('OnlyBrowserDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlyBrowserDirective();
    expect(directive).toBeTruthy();
  });
});
