import { CurrencyCurrentPipe } from './currency-current.pipe';

describe('CurrencyCurrentPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyCurrentPipe();
    expect(pipe).toBeTruthy();
  });
});
