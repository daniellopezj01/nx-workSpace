import { SavePayingAllPipe } from './save-paying-all.pipe';

describe('SavePayingAllPipe', () => {
  it('create an instance', () => {
    const pipe = new SavePayingAllPipe();
    expect(pipe).toBeTruthy();
  });
});
