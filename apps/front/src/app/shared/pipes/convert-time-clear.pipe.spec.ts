import { ConvertTimeClearPipe } from './convert-time-clear.pipe';

describe('ConvertTimeClearPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertTimeClearPipe();
    expect(pipe).toBeTruthy();
  });
});
