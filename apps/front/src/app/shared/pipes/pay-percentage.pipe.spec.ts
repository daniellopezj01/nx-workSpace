import { PayPercentagePipe } from './pay-percentage.pipe';

describe('PayPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new PayPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
