import { RouteTranslatePipe } from './route-translate.pipe';

describe('RouteTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new RouteTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
