import { SmallPercentagePipe } from './small-percentage.pipe';

describe('SmallPercentagePipe', () => {
  it('create an instance', () => {
    const pipe = new SmallPercentagePipe();
    expect(pipe).toBeTruthy();
  });
});
