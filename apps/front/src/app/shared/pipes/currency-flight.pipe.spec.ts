import { CurrencyFlightPipe } from './currency-flight.pipe';

describe('CurrencyFlightPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyFlightPipe();
    expect(pipe).toBeTruthy();
  });
});
