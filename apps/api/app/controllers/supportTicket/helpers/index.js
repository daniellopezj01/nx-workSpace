const { createObjectMessage } = require('./createObjectMessage')
const { createObjectReservation } = require('./createObjectReservation')

module.exports = {
  createObjectMessage,
  createObjectReservation
}
