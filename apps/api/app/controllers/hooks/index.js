const { getContractDeparture } = require('./getContractDeparture')
const { getWallet } = require('./getWallet')
const { createAvatar } = require('./createAvatar')
const { createHook } = require('./createHook')
const { deleteHook } = require('./deleteHook')

module.exports = {
  getContractDeparture,
  getWallet,
  createAvatar,
  createHook,
  deleteHook
}
